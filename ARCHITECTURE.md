# Linux-server — معماری پایدار (Persistent Immutable Architecture)

## هدف
یک سرور Ubuntu 22.04 که توسط GitHub Actions ساخته می‌شود و هر چند ساعت با یک
runner جدید جایگزین می‌شود، اما **همهٔ داده‌ها، برنامه‌ها، تنظیمات، کلید SSH و
آدرس Tailscale آن همیشه ثابت می‌مانند** — چه با ریست خودکار، چه با cancel دستی
و run مجدد.

## ایدهٔ اصلی
runner موقتی است؛ بنابراین **حافظهٔ دائمی باید بیرون از runner** باشد. در این
معماری از خودِ GitHub استفاده شده است و به هیچ سرویس خارجی نیازی نیست:

| جزء | مکانیزم |
|---|---|
| داده‌ها و فایل‌ها | آرشیو `state.tar.gz` روی یک **Release چرخشی** (tag: `state`) |
| برنامه‌های نصب‌شده | لیست پکیج‌ها (`dpkg --get-selections`) داخل همان آرشیو |
| کلید SSH | کلید عمومی ثابت در مخزن + کلید خصوصی نزد کاربر |
| آدرس Tailscale | **IP ثابت** با Tailscale API + حذف nodeهای قدیمی |
| جلوگیری از تداخل | `concurrency` (همیشه فقط یک سرور فعال) |

## چرخهٔ حیات (Lifecycle)

```
 cron هر ۵ ساعت
      │
      ▼
 [checkout] ──► [restore.sh] ──► [setup + کاربر + SSH + Tailscale] ──► [زنده ماندن ~۵.۵ ساعت]
                    ▲                                                            │
                    │        (هر ۳۰ دقیقه save.sh — آپلود روی Release)            │
                    └────────────────────────────────────────────────────────────┘
                                             │
                                             ▼
                                [Final save — حتی در صورت cancel]
```

1. **Restore** — ابتدای هر run، آخرین `state.tar.gz` از Release دانلود می‌شود:
   پکیج‌ها نصب، فایل‌ها و تنظیمات بازگردانی می‌شوند.
2. **Run** — سرور ساخته می‌شود و هر ۳۰ دقیقه وضعیتش ذخیره می‌شود.
3. **Save نهایی** — با `if: always()` حتی اگر run به صورت دستی cancel شود،
   آخرین وضعیت ذخیره می‌شود. بنابراین «کنسل + run مجدد» هیچ داده‌ای را از بین نمی‌برد.
4. **جایگزینی بدون وقفه** — cron هر ۵ ساعت یک run صف می‌کند؛ به کمک
   `concurrency: cancel-in-progress: false`، run جدید منتظر پایان run قبلی
   می‌ماند و بلافاصله بعد از آن شروع می‌شود (بدون همپوشانی، بدون تداخل).

## آنچه ذخیره می‌شود (persist.list)
- `/home/Hamid` و `/root` (فایل‌های شخصی)
- `/etc` (تنظیمات سیستم، به‌جز فایل‌های حساس میزبان)
- `/opt`، `/srv`، `/usr/local` (برنامه‌های خارج از apt)
- `/var/www` (وب‌اپ‌ها) و `/var/spool/cron` (زمان‌بندی‌ها)
- لیست کامل پکیج‌ها

برای افزودن مسیر جدید کافی است یک خط به `.github/scripts/persist.list` اضافه کنید.

## SSH ثابت
- کلید عمومی `ed25519` در `.github/ssh/id_ed25519.pub` (مخزن) — روی سرور به
  `authorized_keys` تبدیل می‌شود.
- کلید خصوصی **هرگز در مخزن نیست** و فقط نزد کاربر است؛ بنابراین هر run جدید با
  همان کلید همیشگی قابل دسترسی است و کلید با reset عوض نمی‌شود.
- کلیدهای host هم داخل state ذخیره می‌شوند تا fingerprint سرور ثابت بماند.

## Tailscale IP ثابت
- اتصال با `TAILSCALE_AUTH_KEY` و hostname ثابت `linux-server-vps`.
- با `TAILSCALE_API_TOKEN`:
  1. nodeهای قدیمی هم‌نام حذف می‌شوند،
  2. نام دقیق اعمال می‌شود (جلوگیری از پسوند `-1`، `-2`...)،
  3. IP مشخص‌شده در `TAILSCALE_FIXED_IP` تثبیت می‌شود.
- نتیجه: هم IP ثابت است و هم MagicDNS (`linux-server-vps.<tailnet>.ts.net`).

## Secrets موردنیاز (Actions → Secrets)
| Secret | توضیح |
|---|---|
| `TAILSCALE_AUTH_KEY` | کلید auth تیل‌اسکیل برای اتصال به tailnet |
| `TAILSCALE_API_TOKEN` | توکن API برای تثبیت IP (اختیاری اما لازم برای IP ثابت) |
| `TAILSCALE_FIXED_IP` | IP ثابت موردنظر (پیش‌فرض `100.100.100.100`) |
| `HAMID_PASSWORD` | پسورد کاربر Hamid |

> کلید خصوصی SSH و پسوردها **هرگز** در مخزن commit نمی‌شوند.

## نکات امنیتی
- مخزن باید **Private** باشد.
- اگر کلید خصوصی یا پسوردی لو رفت، کافی است: کلید عمومی را در مخزن عوض کنید و
  `HAMID_PASSWORD` را در Secrets به‌روز کنید — معماری نیازی به تغییر ندارد.
