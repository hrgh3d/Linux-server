# Linux-server — معماری پایدار (Persistent Immutable Architecture)

## هدف
یک سرور Ubuntu 22.04 که توسط GitHub Actions ساخته می‌شود و هر چند ساعت با یک
runner جدید جایگزین می‌شود، اما **همهٔ داده‌ها، برنامه‌ها، تنظیمات، کلید SSH و
آدرس Tailscale آن همیشه ثابت می‌مانند** — چه با ریست خودکار، چه با cancel دستی
و run مجدد.

## ایدهٔ اصلی
runner موقتی است؛ بنابراین **حافظهٔ دائمی باید بیرون از runner** باشد — و برای
اینکه حتی با حذف workflow یا مخزن اصلی هم از بین نرود، در یک **مخزن جداگانهٔ
اختصاصی** نگهداری می‌شود:

| جزء | مکانیزم |
|---|---|
| داده‌ها و فایل‌ها | آرشیو `state.tar.gz` روی Release چرخشی (tag `state`) در مخزن **`Linux-server-state`** |
| برنامه‌های نصب‌شده | لیست پکیج‌ها (`dpkg --get-selections`) داخل همان آرشیو |
| کلید SSH | کلید عمومی ثابت در مخزن کد + کلید خصوصی نزد کاربر |
| آدرس Tailscale | **IP ثابت** با Tailscale API + حذف nodeهای قدیمی |
| جلوگیری از تداخل | `concurrency` (همیشه فقط یک سرور فعال) |

> مخزن `Linux-server-state` فقط داده نگه می‌دارد. حتی اگر workflow یا کل مخزن
> کد حذف شود، داده‌ها در مخزن state باقی می‌مانند.

## چرخهٔ حیات (Lifecycle)

```
 cron هر ۵ ساعت
      │
      ▼
 [checkout] ──► [setup] ──► [restore.sh از مخزن state] ──► [کاربر + SSH + Tailscale]
                                                                    │
                                            [زنده ماندن ~۵.۵ ساعت]  │
                                                                    ▼
                                    [Final save — در پایان run، در timeout و در cancel دستی]
```

1. **Restore** — ابتدای هر run، آخرین `state.tar.gz` از Release مخزن state دانلود
   می‌شود: پکیج‌ها نصب، فایل‌ها و تنظیمات بازگردانی می‌شوند.
2. **Run** — سرور ساخته می‌شود.
3. **Save نهایی** — فقط در لحظهٔ تعویض سرور (پایان run). با `if: always()` حتی اگر
   run دستی cancel شود هم اجرا می‌شود. بنابراین «کنسل + run مجدد» داده‌ای از بین
   نمی‌برد و **بکاپ تکراری/الکی انباشته نمی‌شود** (همیشه فقط یک نسخهٔ rolling).
4. **جایگزینی بدون وقفه** — `concurrency: cancel-in-progress: false` باعث می‌شود
   run جدید منتظر پایان run قبلی بماند و بلافاصله بعد از آن شروع شود.

## آنچه ذخیره می‌شود (persist.list)
- `/home/Hamid` و `/root` (فایل‌های شخصی)
- `/etc` (تنظیمات سیستم، به‌جز فایل‌های حساس میزبان)
- `/opt`، `/srv`، `/usr/local` (برنامه‌های خارج از apt)
- `/var/www` (وب‌اپ‌ها) و `/var/spool/cron` (زمان‌بندی‌ها)
- لیست کامل پکیج‌ها

برای افزودن مسیر جدید کافی است یک خط به `.github/scripts/persist.list` اضافه کنید.

## SSH ثابت
- کلید عمومی `ed25519` در `.github/ssh/id_ed25519.pub` (مخزن کد) — روی سرور به
  `authorized_keys` تبدیل می‌شود.
- کلید خصوصی **هرگز در هیچ مخزنی نیست** و فقط نزد کاربر است؛ بنابراین هر run جدید
  با همان کلید همیشگی قابل دسترسی است.
- کلیدهای host هم داخل state ذخیره می‌شوند تا fingerprint سرور ثابت بماند.

## Tailscale IP ثابت
- اتصال با `TAILSCALE_AUTH_KEY` و hostname ثابت `linux-server-vps`.
- با `TAILSCALE_API_TOKEN`:
  1. nodeهای قدیمی هم‌نام حذف می‌شوند،
  2. نام دقیق اعمال می‌شود (جلوگیری از پسوند `-1`، `-2`...)،
  3. IP مشخص‌شده در `TAILSCALE_FIXED_IP` تثبیت می‌شود.
- نتیجه: هم IP ثابت است و هم MagicDNS (`linux-server-vps.<tailnet>.ts.net`).

## دسترسی بدون رمز برای مدیریت
- کاربر `Hamid` عضو sudo است و **`sudo su` بدون رمز** کار می‌کند
  (`/etc/sudoers.d/hamid` → `NOPASSWD:ALL`).
- حساب root به صورت پیش‌فرض بدون رمز است (ورود SSH به root غیرفعال است).

## Secrets موردنیاز (Actions → Secrets)
| Secret | توضیح |
|---|---|
| `PERSIST_TOKEN` | توکنی که به مخزن `Linux-server-state` دسترسی نوشتن دارد |
| `TAILSCALE_AUTH_KEY` | کلید auth تیل‌اسکیل برای اتصال به tailnet |
| `TAILSCALE_API_TOKEN` | توکن API برای تثبیت IP ثابت |
| `TAILSCALE_FIXED_IP` | IP ثابت موردنظر (پیش‌فرض `100.100.100.100`) |
| `HAMID_PASSWORD` | پسورد کاربر Hamid (برای ورود SSH؛ sudo بدون رمز است) |

> کلید خصوصی SSH و توکن‌ها **هرگز** در مخزن commit نمی‌شوند.

## نکات امنیتی
- هر دو مخزن (کد و state) باید **Private** باشند.
- در صورت لو رفتن کلید یا پسورد، کافی است کلید عمومی را در مخزن عوض کنید و
  `HAMID_PASSWORD` را در Secrets به‌روز کنید — معماری نیازی به تغییر ندارد.
